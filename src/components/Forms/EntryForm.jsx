import React from 'react'

export default function GuestForm() {
    // onSubmit handler to set custom hook state.
    
    return (
        <div>
        {/* 
            - fieldset.
            - legend.
            - labels with htmlFor which matches the id of relative input.
            - inputs with ids that match the labels htmlFor.
                - One for name
                - One for guest entry 
            - submit button/input
        */}
        </div>
    )
}
